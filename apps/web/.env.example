# Frontend (apps/web) — Vite Environment Variables
# All variables must be prefixed with VITE_ to be exposed to the browser.
#
# Usage:
#   Local dev  →  copy to .env.local (gitignored), run: pnpm dev
#   QA build   →  CI injects VITE_* env vars + runs: pnpm build:qa
#   Prod build →  CI injects VITE_* env vars + runs: pnpm build:prod
#
# See also: .env.local.example, .env.qa.example, .env.prod.example

# Application environment: local | qa | prod
VITE_APP_ENV=local

# API Gateway base URL (Vite dev proxy target in local; ALB/CloudFront domain in qa/prod)
VITE_API_BASE_URL=http://localhost:3000

# LaunchDarkly client-side key (from LaunchDarkly dashboard)
VITE_LAUNCHDARKLY_CLIENT_KEY=<your-launchdarkly-client-key>

# Sentry DSN for error tracking (browser SDK)
VITE_SENTRY_DSN=<your-sentry-dsn>

# PostHog for analytics (browser SDK)
VITE_POSTHOG_KEY=<your-posthog-key>
VITE_POSTHOG_HOST=https://app.posthog.com
